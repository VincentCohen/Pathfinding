class Pathfinder {

    constructor (canvas) {
        let grid = new Grid(canvas);

        // Run every x and redraw
        this.update();
    }

    update () {
        cells();
    }

    cells() {
        // Draw every of every itteration
    }

}
 